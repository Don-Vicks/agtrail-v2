---
description: Start the development server with HMR
---

# Start Dev Server

// turbo-all

1. Start the React Router dev server:

```bash
pnpm dev
```

The app will be available at `http://localhost:5173` with hot module replacement enabled.
